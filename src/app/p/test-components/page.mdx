import {BtnCount} from '@/components/BtnCount';
import { posts } from '@/utils/posts';

export const metadata = {
  title: posts[1].title,
  desciption: posts[1].description,
};

# Test components

Yes, using MDX we can use any React component.

### This is a React component SSG

<BtnCount />